# antigravity-cli

A lightweight Docker-based environment for Google's Antigravity CLI, allowing you to run the agent harness securely with D-Bus and Gnome Keyring support.

### Run the CLI
Use the runner script to interact with the Antigravity agent:
```bash
./scripts/run.sh
```
